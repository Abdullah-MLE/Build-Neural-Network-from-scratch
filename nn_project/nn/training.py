"""Training loop and evaluation helpers."""


def train(model, features, labels, loss_fn, optimizer, epochs, batch_size):
    pass


def evaluate(model, features, labels, loss_fn):
    pass


def accuracy(predictions, labels):
    pass
